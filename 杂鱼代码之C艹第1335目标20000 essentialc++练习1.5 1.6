#pragma once
#include <iostream>
#include <string>

int namerecord();#include <iostream>
#include <string>
using namespace std;
int namerecord()
{
	cout << "please enter your name: ";
	string username;
	cin >> username;
	bool flag = true;
	while (flag)
	{
		if (username.size() < 2)
		{
			cout << "username should beyond two words!!" << endl;
			cout << username.size() << endl;
			return 0;
		}
		else
		{
			cout << "welcome " << username << endl;
			cout << username.size() << endl;
			flag = false;
		}
	}
	system("pause");
	return 0;
}
#include <iostream>
using namespace std;
#include "usname.h"
int main()
{
	while (1)
	{
		namerecord();
	}
}
